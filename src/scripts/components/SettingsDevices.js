import React, { Component } from 'react';

class SettingsDevices extends Component {
  static propTypes = {
  }

  render() {
    return (
      <form className='form'>
      </form>
    );
  }
}

export default SettingsDevices;
